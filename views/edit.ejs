<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p>Edit your chat: <%= chat._id %></p>
    <p>
        this chat was sent to <b><%= chat.to %></b> 
    </p>
    <form action="/chats/<%= chat._id%>?_method=PUT" method="post">
        <textarea name="msg" id="msg" rows="5" cols="50"><%= chat.msg%></textarea>
        <br/>
        <button>Save</button>
    </form>
</body>
</html>